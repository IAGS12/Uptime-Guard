:{$PORT} {
    encode gzip

    # WebSocket: MUST be handled FIRST before php_fastcgi
    handle /app/* {
        reverse_proxy 127.0.0.1:9090
    }

    # PHP: serve Laravel app
    handle {
        root * /app/public
        php_fastcgi 127.0.0.1:9000
        file_server
    }
}
